box: golang
build:
  steps:
    - install-packages:
        packages: openssh-client
    - add-ssh-key:
        keyname: REPORTER_KEY
        host: github.com
    - add-to-known_hosts:
        hostname: github.com
        fingerprint: "16:27:ac:a5:76:28:2d:36:63:1b:56:4d:eb:df:a6:48"
    - script:
        name: fetch github.com/wercker/reporter
        code: git clone git@github.com:wercker/reporter _vendor/src/github.com/wercker/reporter
    - termie/glide-build
    - script:
        name: go vet
        code: |-
          # export GOPATH=$WERCKER_SOURCE_DIR
          # sudo GOPATH=$WERCKER_SOURCE_DIR /usr/local/go/bin/go get code.google.com/p/go.tools/cmd/vet
          go get golang.org/x/tools/cmd/vet
          go vet *.go
    - golint
deploy:
  steps:
    - script:
        name: fake deploy
        code: |-
            ls
