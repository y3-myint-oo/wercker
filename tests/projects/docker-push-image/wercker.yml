build:
  box: golang
  steps:
    - internal/docker-build:
        tag: myrepo1:docker-push-image-tag1  # assumed to be of the form repo:tag 
    - internal/docker-push: 
        image: myrepo1:docker-push-image-tag1
        repository: myrepo2
        tag: docker-push-image-tag2 # cannot include the repo 

