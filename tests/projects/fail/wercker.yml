box: ubuntu
# Build definition
build:
  # The steps that will be executed on build
  steps:
    - script:
        name: "i am a display name"
        code: "false"
  after-steps:
    - script:
        code: |-
          echo "lala la"
