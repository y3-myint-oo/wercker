build:
  box: golang
  steps:
    - script:
        name: Run tests
        code: go test ./...     
    - internal/docker-build:
        tag: repo1:docker-build-image-tag1
        buildargs: "foo=val1 bar=val2"

