build:
  box: golang
  steps:
    - script:
        name: Run tests
        code: go test ./...     
    - internal/docker-build:
        image-name: docker-build-image-name-1
        buildargs: "foo=val1 bar=val2"

